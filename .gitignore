node_modules/
.astro/
.pnpm-store/

# Astro build output — deployed via GitHub Actions, not committed
# Leading slash anchors to repo root, so src/content/docs/ stays tracked.
/docs/

# Secrets / local config — never commit
.env
.env.local
.env.*.local
